<input type="checkbox"
       value="@{<%= context.parent_attr_ref 'luckStartingMod' %>}"
       name="<%= context.attr "zeroRangedWeaponLuckyRoll", include_name_prefix: false %>"
       title="Check the box if you have the Lucky Roll for 0-level Starting weapon"
       />
